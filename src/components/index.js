// Tag, GapBox, Section, P, BulletList, Metric
// Extract these from geopolitical-system-v2.jsx and export each as a named export.
// Example:
// export function Tag({ color, children }) { ... }
// export function GapBox({ children }) { ... }
